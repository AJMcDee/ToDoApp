export function add(title,description,dueDate,priorityLevel,complete, project) {
    let projectIndex
    return {
        title,
        description,
        dueDate,
        priorityLevel,
        complete,
        projectIndex
    }
}